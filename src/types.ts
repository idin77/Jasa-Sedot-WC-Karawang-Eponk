export type Language = 'id' | 'en';

export interface Translations {
  header: {
    title: string;
    subtitle: string;
    nav: {
      masalah: string;
      layanan: string;
      keunggulan: string;
      estimasi: string;
    };
    cta: string;
  };
  // Add more sections as needed
}
